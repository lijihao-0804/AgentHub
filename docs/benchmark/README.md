# Benchmark

Benchmark datasets and metrics start in M3. Formal experiments will bind a Git commit,
resolved specification hash, knowledge snapshot, dataset version and pricing snapshot.
M0 has no quality or performance claim.
