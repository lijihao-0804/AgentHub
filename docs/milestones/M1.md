# M1 — Auth / Tenant / RBAC

## 当前状态

M1-A 至 M1-D 的代码实现和 M1-E 的 PostgreSQL 集成测试已提交到
`m1/auth-tenant-rbac`。本地已使用 Docker Desktop 中的真实 PostgreSQL 完成集成验收，
GitHub Actions CI #10 的 backend PostgreSQL job 与 frontend job 也已通过。

## 已实现范围

- Argon2id 密码哈希、短期身份 JWT、opaque refresh token rotation/reuse detection；
- `users`、`auth_sessions`、Organization/Workspace membership 与 `audit_logs`；
- `PrincipalContext → OrganizationContext → WorkspaceExecutionContext`；
- OWNER/ADMIN、DEVELOPER、VIEWER 的服务端权限解析；
- workspace 成员 API、跨租户 404、最后一个 Owner 的并发锁定保护；
- 真实 PostgreSQL 集成测试入口，禁止用 SQLite 替代。

## 验收结论

| 项目 | 结果 |
|---|---|
| Unit/contract/API tests | PASS |
| Ruff | PASS |
| Alembic offline SQL 检查 | PASS |
| PostgreSQL migration/Auth/Tenant/RBAC integration | PASS（9 passed） |
| GitHub Actions PostgreSQL job | PASS |
| M1 acceptance | PASS |

可用以下命令复核真实 PostgreSQL 集成：

```powershell
$env:AGENTHUB_TEST_DATABASE_URL = "postgresql+asyncpg://agenthub:agenthub@127.0.0.1:5432/agenthub_test"
uv run --locked pytest -m integration
```

该命令必须连接真实 PostgreSQL；不得用 SQLite 替代。GitHub Actions CI #10 已通过，M1
可以正式标记为 `PASS`。
