# M2 — ModelGateway + Capability Contract

## 当前状态

M2-A 至 M2-F 已在 `m2/model-gateway` 完成实现，等待最终 GitHub Actions 验收后正式标记
`PASS`。本阶段严格停在 ModelGateway 边界，不进入 M3。

## 已实现范围

- provider-neutral 的 generate/stream/usage/cost/health/capabilities 合约；
- `provider_credentials`、`model_profiles` 与 Alembic migration `0003`；
- 显式 `WorkspaceExecutionContext` 查询和数据库复合外键，确保 credential/profile/fallback 的
  workspace 隔离；
- DeepSeek 与 OpenAI-compatible 的 LiteLLM provider adapter；
- timeout、有限 retry，以及首个可见 token 之前的 fallback；首个可见 delta 之后禁止透明切换；
- disabled profile/credential、capability mismatch、health failure 和 fallback cycle 的安全边界；
- `model.generate` TraceSink span，usage/cost 只以安全摘要进入 trace，TraceSink 失败不影响模型请求。

## 明确不在 M2

- 不创建 Agent、RAG、Tool Runtime、Approval 或业务 Run；
- 不创建 `model_invocations` 表或正式业务调用持久化；
- 不增加 Redis/Qdrant 依赖，也不要求本地 Docker；
- 不修改 M3-M8 的业务 scope；
- 不提前实现 M5 的 LangGraph PostgreSQL checkpoint bootstrap。

## 验收边界

| 项目 | 结果 |
|---|---|
| M2 单元/合约/适配器/安全边界测试 | PASS（26 passed） |
| Ruff | PASS |
| Alembic offline SQL / migration 静态检查 | PASS |
| 全仓 pytest | PASS（45 passed，11 skipped） |
| frontend build | PASS |
| GitHub Actions backend PostgreSQL 与 frontend build | 待最终提交 CI |
| `git diff --check` 与干净工作树 | 待最终提交确认 |
| M2 acceptance | 待 GitHub Actions 通过 |

完成最终 CI 验收并确认工作树干净后，M2 才可正式标记为 `PASS`；随后停止，不进入 M3。
